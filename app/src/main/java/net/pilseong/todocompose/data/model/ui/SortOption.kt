package net.pilseong.todocompose.data.model.ui

import net.pilseong.todocompose.R

enum class SortOption(val label: Int) {
    DESC(label = R.string.badge_order_desc_label),
    ASC(label = R.string.badge_order_asc_label),
}